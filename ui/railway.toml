# CDP UI (staging.neosofia.tech)
#
# Railway service settings (dashboard):
#   Root Directory: /ui
#   Config file path: /ui/railway.toml
#   Autodeploy: ON — Railway deploys via GitHub callback after Wait for CI passes
#   Wait for CI: ON — gates on push workflows (CodeQL + cdp-ui-quality-staging), not post-deploy E2E
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
