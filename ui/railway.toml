# CDP UI (staging.neosofia.tech)
#
# Railway service settings (dashboard):
#   Root Directory: /ui
#   Config file path: /ui/railway.toml
#   Autodeploy: OFF — .github/workflows/cdp-ui-deploy-staging.yml deploys after TypeScript CI
#   Wait for CI: OFF — post-deploy E2E must not gate the build (see OPERATIONS.md)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
