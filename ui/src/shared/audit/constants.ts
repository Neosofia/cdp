/** Rows per page in audit history sheets. */
export const AUDIT_PAGE_SIZE = 20;

/** Page size when fetching all audit rows for CSV export. */
export const AUDIT_CSV_FETCH_PAGE_SIZE = 100;

/** Safety cap on CSV export pagination (page count). */
export const AUDIT_CSV_MAX_PAGES = 100;
