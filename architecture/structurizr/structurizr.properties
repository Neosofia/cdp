# Structurizr data-directory settings (dev compose mount / production COPY).
# Production overrides editable via -Dstructurizr.editable=false in start.sh.
structurizr.editable=true
structurizr.workspace.maxsize=10MB
