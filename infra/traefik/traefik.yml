# Traefik static configuration — local dev only
api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true   # hot-reload when dynamic/*.yml changes

log:
  level: INFO
